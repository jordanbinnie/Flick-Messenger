import { Circle } from "better-react-spinkit"

function Loading() {
    return (
        <div className="loading-spinner-container">
                <Circle color="#2e9bff" size={100} />
        </div>
    )
}

export default Loading